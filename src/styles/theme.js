import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    text: {
      primary: '#82888C'
    }
  },
  icons: {
    denim: '#1976d2',
    yellow: '#f9cb40'
  }
});

export default theme;
